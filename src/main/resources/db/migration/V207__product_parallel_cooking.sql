-- Не всякий товар занимает руки баристы. Сэндвич греется в печи, десерт лежит готовым
-- в витрине — такие позиции готовятся одновременно с остальным заказом и не должны
-- складываться с ним в общее время. Товары без флага считаются как раньше — суммой.

ALTER TABLE public.ducks_coffee_shop_product_table
    ADD COLUMN cooks_in_parallel boolean DEFAULT false NOT NULL;
