-- Напоминание продавцу о просроченном заказе: принят, но через минуту после обещанного
-- времени готовности так и не помечен готовым. Флаг нужен, чтобы напомнить один раз, —
-- проверка крутится каждую минуту и без него слала бы пуш на каждом тике.

ALTER TABLE public.ducks_coffee_orders_table
    ADD COLUMN is_late_notified boolean DEFAULT false NOT NULL;
