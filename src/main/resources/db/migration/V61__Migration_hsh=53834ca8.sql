CREATE TABLE IF NOT EXISTS ducks_coffee_shop_schedule_table (id BIGSERIAL PRIMARY KEY, shop_id BIGINT NOT NULL, "dayOfWeek" INT NOT NULL, "startTime" TEXT NOT NULL, "endTime" TEXT NOT NULL, "isClosed" BOOLEAN DEFAULT FALSE NOT NULL, CONSTRAINT fk_ducks_coffee_shop_schedule_table_shop_id__id FOREIGN KEY (shop_id) REFERENCES ducks_coffee_shop_table(id) ON DELETE RESTRICT ON UPDATE RESTRICT)