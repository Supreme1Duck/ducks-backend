-- Режим приготовления кофейни: сколько бариста на смене и как они разбирают заказ.
-- 0 — обычный, один бариста собирает позиции по очереди (поведение до этой миграции).
-- 1 — x2, двое делят один заказ между собой.
-- Переключается селлером по ходу смены и влияет только на новые заказы: уже принятые
-- хранят своё время в ducks_coffee_orders_table.time_to_cook_in_minutes и не пересчитываются.

ALTER TABLE public.ducks_coffee_shop_table
    ADD COLUMN cooking_mode integer DEFAULT 0 NOT NULL;
